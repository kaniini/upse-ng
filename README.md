<!-- SPDX-License-Identifier: LGPL-2.1-or-later -->
# UPSE-NG

UPSE-NG is a Rust library for playing PSF and PSF2 music files. The emulator is
split into reusable components under `crates/` and provides both Rust and C
interfaces.

Run the test suite with:

```sh
make check
```
